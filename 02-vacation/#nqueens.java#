public class nqueens {

    public char[][] board;
    public int queens;

    public nqueens(int n) {
	board = new int[n][n];
	queens = n;
    }

    public boolean check(int[] row, int num) {
	for (int i = 0; i < board.length; i++) {
	    if (row[i] == row[num]) {
		return false;
	    }
	    if (row[i] 
    }

}
